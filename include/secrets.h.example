/*
    secrets.h.example - Template for secrets.h
    
    Copy this file to secrets.h and fill in your actual credentials.
    secrets.h is excluded from version control for security.
*/

#pragma once

// Twilio API Credentials (for SMS notifications)
constexpr const char* TWILIO_ACCOUNT_SID = "your_account_sid";
constexpr const char* TWILIO_AUTH_TOKEN = "your_account_token";

// Note: The following settings are configured via the web interface (Debug & Calibration page):
// - SMS phone number (stored in NVS)
// - Discord webhook URL (stored in NVS)
//
// To set up Discord webhook:
// 1. In Discord, go to Server Settings > Integrations > Webhooks
// 2. Create a new webhook and copy the URL
// 3. Enter the URL in the Debug & Calibration page under "Notification Settings"
