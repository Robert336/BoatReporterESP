/*
    secrets.h.example - Template for secrets.h

    Copy this file to secrets.h.
    secrets.h is excluded from version control.

    NOTE: Twilio credentials (account SID, auth token, messaging service SID)
    are configured at runtime via the web UI (Notifications page → SMS · Twilio)
    and stored in NVS.  No compile-time secrets are required for SMS.
*/

#pragma once

// No compile-time secrets are currently required.
// All notification channel credentials (SMS/Twilio, Discord, Custom HTTP)
// are configured via the web UI and persisted in NVS under the "notify" namespace.
